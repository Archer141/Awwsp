@model IEnumerable<Awwsp.Models.ApplicationUser>

@{
    ViewBag.Title = "AllUsers";
    Layout = "~/Views/Shared/Layouts/_Layout.cshtml";
}
    <div class="container-fluid">
        <div class="text-center">
            <h2 class="display-3 mt-2">All registred users</h2>

            <p>
                @Html.ActionLink("Create New", "Account",null,new { @class="btn btn-outline-info"})
            </p>

        </div>
        <div class="table-responsive-xl ">
            <table id="tab" class="table table-striped table-bordered table-sm" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <th scope="col" class="th-sm">
                            @Html.DisplayNameFor(model => model.FirstName)
                        </th>
                        <th scope="col"class="th-sm">
                            @Html.DisplayNameFor(model => model.LastName)
                        </th>
                        <th scope="col"class="th-sm">
                            @Html.DisplayNameFor(model => model.Email)
                        </th>

                        <th scope="col"class="th-sm">
                            @Html.DisplayNameFor(model => model.SecurityStamp)
                        </th>
                        <th scope="col"class="th-sm">
                            @Html.DisplayNameFor(model => model.PhoneNumber)
                        </th>

                        <th scope="col"class="th-sm">
                            @Html.DisplayNameFor(model => model.UserName)
                        </th>
                        <th scope="col"class="th-sm">Zarządzanie</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.FirstName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.LastName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Email)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.SecurityStamp)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.PhoneNumber)
                            </td>
                          
                            <td>
                                @Html.DisplayFor(modelItem => item.UserName)
                            </td>
                            <td>
                                <div class=" btn-group-sm">
                                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-outline" })
                                    @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-outline" })
                                    @if (item.UserName != User.Identity.Name)
                                    {
                                        @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-outline" })
                                    }
                                    else
                                    {
                                        @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-outline disabled" })

                                    }
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
              
            </table>
        </div>
    </div>
<script>
    $(document).ready(function () {
        $('#dtBasicExample').DataTable();
        $('.dataTables_length').addClass('bs-select');
    });
</script>

<script>
    $(document).ready(function () {
        $('#tab').DataTable();
        $('.dataTables_length').addClass('bs-select');
    });
</script>
